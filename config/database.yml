default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: oobarikuto
  password: Sausupo0404?
  host: localhost

development:
  <<: *default
  database: postgresql

test:
  <<: *default
  database: postgresql

production:
  <<: *default
  database: postgresql
